export type authUserModelProps = {
  UserID: string;
  email: string;
  name: string;
  user: string;
};

export type createUserModelProps = {
  email: string;
  name: string;
  password: string;
  user: string;
};
