export type authUserModelProps = {
  UserID: string;
  firstName: string;
  lastName: string;
  email: string;
  user: string;
};

export type createUserModelProps = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  user: string;
};
